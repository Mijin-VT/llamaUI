"""llamaUI Qt application package.

The Phase 2 skeleton ships:
- a dark KDE-friendly theme (see ``app/theme.py``)
- a dense shell with sidebar, header, stacked pages, and inspector
- six placeholder pages (Library, Discover, Run, Profiles, Settings, Diagnostics)
- reusable widgets and page base classes
- stubs for service modules (HF, downloads, library scan, llama-server)
  ready for Phase 3+ implementation

Run with ``python qt_app/main.py`` or ``python -m qt_app``.
"""
